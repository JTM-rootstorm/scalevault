# Optional status UI

This directory is reserved for a small installation status component. It must
display node connectivity and ingress state without rendering memory bodies or
exposing operator controls.
