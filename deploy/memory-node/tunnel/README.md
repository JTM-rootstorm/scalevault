# Secure MCP Tunnel profile

This directory will contain the private OpenAI tunnel configuration after the
Milestone 0 capability probe. Runtime keys belong in systemd credentials, not
in this repository. This profile is not a public plugin endpoint.
