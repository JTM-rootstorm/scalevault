# Relay authorization facade

Use a maintained identity provider and a narrow MCP-compatible authorization
facade. Required controls include PKCE, resource/audience validation, exact
redirect URIs, state validation, short-lived access tokens, and revocation.
