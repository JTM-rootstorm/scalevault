# Plugin submission

Public submission is optional. A candidate must use the stable generic relay,
contain no private coordinates or secrets, publish privacy and deletion terms,
and pass current capability and review checks. Secure MCP Tunnel is reserved for
private connectivity.
