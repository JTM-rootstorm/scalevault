# Public lineage

A public seed is a deterministic, explicitly approved transformation of
`public_seed` memories. It excludes restricted evidence and private identifiers,
runs leakage checks, and publishes to a separate repository or protected branch.
Child branches inherit auditable ancestry without implying subjective continuity.
